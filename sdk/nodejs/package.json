{
    "name": "@pulumi/kubernetes-xyz",
    "version": "${VERSION}",
    "keywords": [
        "pulumi",
        "kubernetes",
        "xyz",
        "kind/component",
        "category/infrastructure"
    ],
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource kubernetes-xyz ${VERSION}"
    },
    "dependencies": {
        "@pulumi/kubernetes": "^3.7.1",
        "@pulumi/pulumi": "^3.42.0"
    },
    "devDependencies": {
        "@types/node": "^14",
        "typescript": "^3.7.0"
    },
    "pulumi": {
        "resource": true,
        "name": "kubernetes-xyz"
    }
}
